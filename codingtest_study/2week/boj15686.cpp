#include <iostream>
using namespace std;

int main()// 너비탐색
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	int n,m;
	cin >> n >> m;
	int board[n+1][n+1];
	for(int i=1;i<=n;i++)
		for(int j=1;j<=n;j++)
			cin >> board[i][j];
	
}