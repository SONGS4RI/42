#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    long long n;
    long long high = LONG_LONG_MAX, low = 0, middle;

    cin >> n;
    
    cout << res;
}